services:
  mediacleaner:
    image: ghcr.io/drextrime/mediacleaner:latest
    ports:
      - "8080:8080"
    environment:
      - TAUTULLI_BASE_URL=${TAUTULLI_BASE_URL}
      - TAUTULLI_API_KEY=${TAUTULLI_API_KEY}
      - API_KEY=${API_KEY}